#include <stdio.h>
#include <string.h>

int main() {
    char s[20] = "Programming";
    printf("Length = %lu", strlen(s));
    return 0;
}
// Program to find the length of a string using strlen function